# BUSY Bar address. Copy this file to .env and set your device's address.
VITE_BUSY_ADDR=localhost

# Weather backend base URL, without a trailing slash. Set in CI.
VITE_WEATHER_API=https://api.example.com
